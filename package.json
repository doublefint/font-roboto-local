{
  "name": "@polymer/font-roboto-local",
  "version": "1.1.0",
  "private": true,
  "description": "An HTML Import for Robot, with local font files",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@polymer/gen-typescript-declarations": "^1.1.1",
    "bower": "^1.8.0"
  },
  "scripts": {
    "update-types": "bower install && gen-typescript-declarations --deleteExisting --outDir ."
  }
}
